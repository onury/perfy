{
  "extends": "tsconfig-oy",
  "compilerOptions": {
    "ignoreDeprecations": "6.0",
    // ESNext.Disposable adds `Symbol.dispose` + the `Disposable` interface used
    // by `measure()`/`using`. Node >= 22 (the engines floor) provides them natively.
    "lib": ["ES2022", "ESNext.Disposable"],
    "isolatedModules": true,
    "verbatimModuleSyntax": true,
    "incremental": false,
    "outDir": "lib",
    "declarationDir": "lib",
    "declarationMap": true,
    "rootDir": ".",
    // No `typeRoots`: restricting it (e.g. to ["node_modules/@types"]) breaks
    // resolution of package-subpath type entries like "vitest/globals", which
    // does not live under @types. That surfaces as TS2688 here and TS2593
    // ("Cannot find name 'describe'") across the test files.
    "types": ["node", "vitest/globals"]
  },
  "include": [
    "src/**/*.ts",
    "test/**/*.ts"
  ]
}
